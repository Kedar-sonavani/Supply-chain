# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=supply_chain_tracker
DB_USER=root
DB_PASSWORD=Password

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=7d

# API Keys (we'll add these later when needed)
WEATHER_API_KEY=
GOOGLE_MAPS_API_KEY=
